const input = require('./input');


var i = 0;

//var inputSet = input.realInput;
var inputSet = input.testInput;

var validpasswordcount = 0;

for (i=0; i<inputSet.length; ++i) {
  let pw = inputSet[i].split(':')[1].trim();
  
}
